document.addEventListener("DOMContentLoaded", function() { // Espera a que el DOM esté listo
    const formulario = document.getElementById("formulario_usuario"); // Referencia al formulario de registro
    const registro = document.getElementById("boton_registro"); // Referencia al botón de registro
    const botonregistro = document.getElementById("button_register"); // Referencia al botón de envío del formulario de registro
    const inicioSesion = document.getElementById("boton_inicio_sesion"); // Referencia al botón de inicio de sesión
    const botonInicioSesion = document.getElementById("button_signing"); // Referencia al botón de envío del formulario de inicio de sesión
});

    